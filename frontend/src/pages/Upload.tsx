export default function Upload() {
  return (
    <section>
      <h1 className="text-2xl font-semibold mb-2">Upload</h1>
      <p className="text-neutral-600">
        Audio file upload + clip trimmer will live here. After submit, the user is
        redirected to <code>/result/:jobId</code>.
      </p>
    </section>
  )
}
